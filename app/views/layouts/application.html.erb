<!DOCTYPE html>
<html>
<head>
  <title>Scholarize</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag :defaults %>

  <%= csrf_meta_tags %>
</head>
<body>
<%= render 'layouts/messages' %>
<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href='/'>Scholarize</a>

      <div class="nav-collapse">
        <ul class="nav">
          <li><a href="https://github.com/ermauliks/scholarize/blob/master/README.md">About</a></li>
        </ul>
      </div>
      <div id="student_nav" class="navbar-text pull-right">
        <% if student_signed_in? %>
            Signed in as <%= current_student.email %>. Not You ?
            <%= link_to 'Sign out', destroy_student_session_path %>
        <% else %>
            <%= link_to 'Register', new_student_registration_path %>
            or <%= link_to 'Sign in', new_student_session_path %>
        <% end %>
      </div>
    </div>
  </div>
  <!--navbar-inner-->
</div>
<!--navbar navbar-fixed-top-->
<div class="container">
  <div class="container-fluid">
    <div class="row-fluid">
      <%= yield %>
    </div>
  </div>
</div>

</body>
</html>
